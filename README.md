# Illusionsms
Impulse is a well-maintained Python DoS toolkit that includes SMS flooding and call flooding capabilities using public APIs. It works on Linux and Feature	Description
Professional UI	Color-coded terminal interface with full banner, status bars, and progress indicators
Multi-threaded	Configurable workers for concurrent attacks
API Rotation	Cycles through multiple SMS API endpoints automatically
Proxy Support	Load proxies from file for IP rotation
User-Agent Rotation	10+ mobile/desktop user agents to avoid fingerprinting
Retry Logic	Automatic retry on failure (3 attempts per request)
Real-time Stats	Live-updating dashboard with success rate bar
Graceful Shutdown	Ctrl+C handled cleanly without killing terminal
Report Export	Auto-saves JSON report with full breakdown
Payload Variety	Random OTP codes, messages, and alert templates
Error Handling	Catches timeouts, connection errors, proxy failures, and malformed responses
Network Check	Validates internet connectivity before launching



